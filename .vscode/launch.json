{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Play in Editor",
            "type": "godot",
            "request": "launch",
            "project": "${workspaceFolder}",
            "launch_scene": "" // Deixar em branco para rodar a cena principal configurada no Godot
        }
    ]
}
